<form method="post" action="{{ path('app_products_delete', {'Productid': product.Productid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.Productid) }}">
    {# <button class="btn">Delete</button> #}
        <button class="btn btn-danger">delete</button>
    
</form>
